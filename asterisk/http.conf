[general]
enabled=yes
bindaddr=0.0.0.0
bindport=8088

; Security: ARI is bound to all interfaces for Docker Compose compatibility
; The orchestrator container needs to access ARI over the Docker network
; For production, consider binding to a private network interface
; and using a reverse proxy with authentication
